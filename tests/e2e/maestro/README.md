# Maestro AUTH-01 flows
#
# Prerequisites: app installed on simulator/emulator (`com.tngble.app`), Maestro CLI.
#
#   maestro test tests/e2e/maestro/login.yaml
#
# Use a dedicated Auth0 E2E tenant only. Social E2E is deferred (flaky); rely on
# `tests/component` + `tests/integration` plus the Phase 9 manual matrix.
